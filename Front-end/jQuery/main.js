(function($ , window , document){

    // $ is now locally scoped
    // listen for the jQuery ready event or the document
    $(function(){
        console.log("its a better approach to write the jQuery code.")
    });

}(window.jQuery, window , document));